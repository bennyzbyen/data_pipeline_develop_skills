interface:
  display_name: "Data Sync Codegen"
  short_description: "Generate DataEngine sync projects"
  default_prompt: "Use $data-sync-codegen to generate a COT-style data synchronization project from this dev_doc.md."
policy:
  allow_implicit_invocation: true
