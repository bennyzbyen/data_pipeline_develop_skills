interface:
  display_name: "Pipeline Excel Builder"
  short_description: "Build DataHub Pipeline Export Excel files"
  default_prompt: "Use $pipeline-excel-builder to generate a Pipeline Export Excel workbook from this DataEngine waterline document."
policy:
  allow_implicit_invocation: true
